{
  "name": "@google-cloud/firestore-samples",
  "private": true,
  "license": "Apache-2.0",
  "author": "Google Inc.",
  "repository": "googleapis/nodejs-firestore",
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "test": "mocha --timeout 600000",
    "quick": "cd ..; npm run compile; cd -; npm install; ts-node quickstart.ts"
  },
  "dependencies": {
    "@google-cloud/firestore": "..",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.0.0"
  }
}
